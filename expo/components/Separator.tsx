import { View } from "react-native";

const Separator = () => {
  return (
    <View
      style={{
        height: 1,
        width: "100%",
        backgroundColor: "#E6E6E6",
        opacity: 0.5,
      }}
    />
  );
};

export default Separator;
